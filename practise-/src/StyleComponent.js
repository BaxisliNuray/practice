import styled, {css} from "styled-components";

export const Title = styled.h1`
font-size:2.5rem;
background-color:#c6e2e9;
font-weight:900;
color:indianred;
`

// props ile style yazmaq(alinmadi sebebini tapa bilmedim)
// ${props => props.$primary && css`
// background: white;
// color: black;
// `}